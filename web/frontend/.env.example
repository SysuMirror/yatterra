# 前端构建期变量 — 复制为 .env 后按需修改
#
#   cp .env.example .env
#
# Vite 在构建时把 VITE_* 内联进产物，改完必须重新 build:stage + publish:spa。
# 不设置时使用 src/lib/site.ts 里的默认值（参考部署）。

#VITE_DOMAIN=ssemarket.cn
#VITE_PUBLIC_HOST=ssemarket.cn
#VITE_PUBLIC_SCHEME=https
