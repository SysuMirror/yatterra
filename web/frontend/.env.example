# 前端构建期变量 — 复制为 .env 后按需修改
#
#   cp .env.example .env
#
# Vite 在构建时把 VITE_* 内联进产物，改完必须重新 build:stage + publish:spa。
# 不设置时使用 src/lib/site.ts 里的默认值（通用占位符 example.com）——
# 真实部署必须在这里给出实际域名，否则产物里会烘焙占位域名。

#VITE_DOMAIN=example.com
#VITE_PUBLIC_HOST=example.com
#VITE_PUBLIC_SCHEME=https
